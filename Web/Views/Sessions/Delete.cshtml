@model SessionDetailsDto

@{
    ViewData["Title"] = "Скасувати сеанс";
}

<h1>Скасувати сеанс</h1>

<p>Ви впевнені, що хочете скасувати сеанс <strong>#@Model.Id</strong>?</p>

<ul>
    <li>ID фільму: @Model.MovieId</li>
    <li>ID залу: @Model.HallId</li>
    <li>Початок: @Model.StartTime</li>
    <li>Кінець: @Model.EndTime</li>
</ul>

<form asp-action="Cancel" asp-route-id="@Model.Id" method="post">
    @Html.AntiForgeryToken()
    <button class="btn btn-danger" type="submit">Так, скасувати</button>
    <a class="btn btn-link" asp-action="Details" asp-route-id="@Model.Id">Назад</a>
</form>
