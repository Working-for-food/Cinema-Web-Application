@using Web.Helpers
@model Infrastructure.Entities.Movie

@{
<<<<<<< HEAD
    ViewData["Title"] = "Деталі фільму";

    var genres = Model.MovieGenres?
        .Select(mg => mg.Genre?.Name)
        .Where(n => !string.IsNullOrWhiteSpace(n))
        .ToList() ?? new();

    var countries = Model.MovieCountries?
        .Select(x => x.Country?.Name ?? x.CountryCode)
        .Where(x => !string.IsNullOrWhiteSpace(x))
        .ToList() ?? new();

    var actors = Model.MovieActors?
        .OrderBy(x => x.CustOrder)
        .Select(x => x.Actor?.FullName ?? x.ActorId.ToString())
        .Where(x => !string.IsNullOrWhiteSpace(x))
        .ToList() ?? new();

    var directors = Model.MovieDirectors?
        .OrderBy(x => x.BillingOrder)
        .Select(x => x.Director?.FullName ?? x.DirectorId.ToString())
        .Where(x => !string.IsNullOrWhiteSpace(x))
        .ToList() ?? new();
=======
    ViewData["Title"] = "Movie details";
    var genres = Model.MovieGenres?.Select(mg => mg.Genre?.Name).Where(n => !string.IsNullOrWhiteSpace(n)).ToList() ?? new();
    var countries = Model.MovieCountries?.Select(x => x.Country?.Name ?? x.CountryCode).ToList() ?? new();
    var actors = Model.MovieActors?.OrderBy(x => x.CustOrder).Select(x => x.Actor?.FullName ?? x.ActorId.ToString()).ToList() ?? new();
    var directors = Model.MovieDirectors?.OrderBy(x => x.BillingOrder).Select(x => x.Director?.FullName ?? x.DirectorId.ToString()).ToList() ?? new();
    var posterUrl = TmdbImage.Build(Model.PosterPath, "w342");
    var backdropUrl = TmdbImage.Build(Model.BackdropPath, "w1280");

>>>>>>> main
}

<section class="py-5 gray-bg">
    <div class="container-fluid">

        <h1 class="section-title text-center mb-5">ДЕТАЛІ ФІЛЬМУ</h1>

        <div class="card shadow-sm col-lg-8 mx-auto">
            <div class="card-header">
                <h5 class="mb-0">
                    <i class="fa-solid fa-circle-info me-2"></i>
                    Інформація про фільм
                </h5>
            </div>

            <div class="card-body p-4">
                <div class="mb-4">

                    <div class="mb-3">
                        <label class="form-label text-muted">
                            <i class="fa-solid fa-clapperboard me-1"></i>
                            Назва
                        </label>
                        <div class="fw-semibold fs-5">@Model.Title</div>
                    </div>

                    <div class="mb-3">
                        <label class="form-label text-muted">
                            <i class="fa-solid fa-calendar-days me-1"></i>
                            Дата виходу
                        </label>
                        <div class="fw-semibold fs-5">
                            @(Model.ReleaseDate?.ToString("dd.MM.yyyy") ?? "-")
                        </div>
                    </div>

                    <div class="mb-3">
                        <label class="form-label text-muted">
                            <i class="fa-solid fa-clock me-1"></i>
                            Тривалість
                        </label>
                        <div class="fw-semibold fs-5">
                            @(Model.Duration.HasValue ? $"{Model.Duration} хв" : "-")
                        </div>
                    </div>

                    <div class="mb-3">
                        <label class="form-label text-muted">
                            <i class="fa-solid fa-video me-1"></i>
                            Режисери
                        </label>
                        <div class="fw-semibold">
                            @(directors.Count == 0 ? "-" : string.Join(", ", directors))
                        </div>
                    </div>

                    <div class="mb-3">
                        <label class="form-label text-muted">
                            <i class="fa-solid fa-tags me-1"></i>
                            Жанри
                        </label>
                        <div class="fw-semibold">
                            @if (genres.Count == 0)
                            {
                                <span class="text-muted">-</span>
                            }
                            else
                            {
                                foreach (var g in genres)
                                {
                                    <span class="badge bg-info text-dark me-1 mb-1">@g</span>
                                }
                            }
                        </div>
                    </div>

                    <div class="mb-3">
                        <label class="form-label text-muted">
                            <i class="fa-solid fa-globe me-1"></i>
                            Країни
                        </label>
                        <div class="fw-semibold">
                            @(countries.Count == 0 ? "-" : string.Join(", ", countries))
                        </div>
                    </div>

                    <div class="mb-3">
                        <label class="form-label text-muted">
                            <i class="fa-solid fa-user-group me-1"></i>
                            Актори
                        </label>
                        <div class="fw-semibold">
                            @(actors.Count == 0 ? "-" : string.Join(", ", actors))
                        </div>
                    </div>

                    <div class="mb-3">
                        <label class="form-label text-muted">
                            <i class="fa-solid fa-ticket me-1"></i>
                            Сеанси
                        </label>
                        <div class="fw-semibold fs-5">
                            @(Model.Sessions?.Count ?? 0)
                        </div>
                    </div>

                    <div class="mb-3">
                        <label class="form-label text-muted">
                            <i class="fa-solid fa-pen-nib me-1"></i>
                            Оригінальна назва
                        </label>
                        <div class="fw-semibold">
                            @(string.IsNullOrWhiteSpace(Model.OriginalName) ? "-" : Model.OriginalName)
                        </div>
                    </div>

                    <div class="mb-3">
                        <label class="form-label text-muted">
                            <i class="fa-solid fa-language me-1"></i>
                            Мова
                        </label>
                        <div class="fw-semibold">
                            @(string.IsNullOrWhiteSpace(Model.Language) ? "-" : Model.Language)
                        </div>
                    </div>

                    <div class="mb-3">
                        <label class="form-label text-muted">
                            <i class="fa-solid fa-link me-1"></i>
                            Трейлер
                        </label>
                        <div class="fw-semibold">
                            @if (string.IsNullOrWhiteSpace(Model.TrailerUrl))
                            {
                                <span class="text-muted">-</span>
                            }
                            else
                            {
                                <a href="@Model.TrailerUrl" target="_blank" rel="noopener">
                                    Відкрити трейлер
                                </a>
                            }
                        </div>
                    </div>

                    <div class="mb-3">
                        <label class="form-label text-muted">
                            <i class="fa-solid fa-image me-1"></i>
                            Шлях до постера
                        </label>
                        <div class="fw-semibold">
                            @(string.IsNullOrWhiteSpace(Model.PosterPath) ? "-" : Model.PosterPath)
                        </div>
                    </div>

                    <div class="mb-3">
                        <label class="form-label text-muted">
                            <i class="fa-solid fa-panorama me-1"></i>
                            Шлях до фону
                        </label>
                        <div class="fw-semibold">
                            @(string.IsNullOrWhiteSpace(Model.BackdropPath) ? "-" : Model.BackdropPath)
                        </div>
                    </div>

                    <div class="mb-0">
                        <label class="form-label text-muted">
                            <i class="fa-solid fa-align-left me-1"></i>
                            Опис
                        </label>
                        <div class="fw-semibold">
                            @(string.IsNullOrWhiteSpace(Model.Description) ? "-" : Model.Description)
                        </div>
                    </div>

                </div>

                <div class="d-flex justify-content-end gap-2 mt-4">
                    <a asp-action="Index" class="btn btn-secondary">
                        <i class="fa-solid fa-arrow-left me-1"></i>
                        Назад
                    </a>

                    <a asp-action="Edit" asp-route-id="@Model.Id" class="btn btn-maroon">
                        <i class="fa-solid fa-pen-to-square me-1"></i>
                        Редагувати
                    </a>
                </div>

            </div>
        </div>

    </div>
</section>

<<<<<<< HEAD
=======
    <div class="card shadow overflow-hidden">
        @if (!string.IsNullOrWhiteSpace(backdropUrl))
        {
            <img src="@backdropUrl" alt="Backdrop" class="w-100" style="max-height:320px;object-fit:cover" />
        }
        <div class="card-body">
                <div class="col-md-4">
                    @if (!string.IsNullOrWhiteSpace(posterUrl))
                    {
                        <img src="@posterUrl" alt="Poster" class="img-fluid rounded shadow-sm" />
                    }
                    else
                    {
                        <div class="bg-light border rounded d-flex align-items-center justify-content-center" style="height:420px">
                            <span class="text-muted">No poster</span>
                        </div>
                    }
                </div>
            <dl class="row mb-0">
                <dt class="col-sm-3">Release date</dt>
                <dd class="col-sm-9">@(Model.ReleaseDate?.ToString("dd.MM.yyyy") ?? "-")</dd>

                <dt class="col-sm-3">Duration</dt>
                <dd class="col-sm-9">@(Model.Duration.HasValue ? $"{Model.Duration} min" : "-")</dd>

                <dt class="col-sm-3">Directors</dt>
                <dd class="col-sm-9">
                    @if (directors.Count == 0)
                    {
                        <span class="text-muted">-</span>
                    }
                    else
                    {
                        @string.Join(", ", directors)
                    }
                </dd>

                <dt class="col-sm-3">Genres</dt>
                <dd class="col-sm-9">
                    @if (genres.Count == 0)
                    {
                        <span class="text-muted">-</span>
                    }
                    else
                    {
                        foreach (var g in genres)
                        {
                            <span class="badge bg-info text-dark me-1">@g</span>
                        }
                    }
                </dd>

                <dt class="col-sm-3">Countries</dt>
                <dd class="col-sm-9">
                    @if (countries.Count == 0)
                    {
                        <span class="text-muted">-</span>
                    }
                    else
                    {
                        @string.Join(", ", countries)
                    }
                </dd>

                <dt class="col-sm-3">Actors</dt>
                <dd class="col-sm-9">
                    @if (actors.Count == 0)
                    {
                        <span class="text-muted">-</span>
                    }
                    else
                    {
                        @string.Join(", ", actors)
                    }
                </dd>

                <dt class="col-sm-3">Sessions</dt>
                <dd class="col-sm-9">@(Model.Sessions?.Count ?? 0)</dd>

                <dt class="col-sm-3">Original name</dt>
                <dd class="col-sm-9">@(string.IsNullOrWhiteSpace(Model.OriginalName) ? "-" : Model.OriginalName)</dd>

                <dt class="col-sm-3">Language</dt>
                <dd class="col-sm-9">@(string.IsNullOrWhiteSpace(Model.Language) ? "-" : Model.Language)</dd>

                <dt class="col-sm-3">Trailer</dt>
                <dd class="col-sm-9">
                    @if (string.IsNullOrWhiteSpace(Model.TrailerUrl))
                    {
                        <span class="text-muted">-</span>
                    }
                    else
                    {
                        <a href="@Model.TrailerUrl" target="_blank" rel="noopener">Open trailer</a>
                    }
                </dd>

                <dt class="col-sm-3">PosterPath</dt>
                <dd class="col-sm-9">@(string.IsNullOrWhiteSpace(Model.PosterPath) ? "-" : Model.PosterPath)</dd>

                <dt class="col-sm-3">BackdropPath</dt>
                <dd class="col-sm-9">@(string.IsNullOrWhiteSpace(Model.BackdropPath) ? "-" : Model.BackdropPath)</dd>

                <dt class="col-sm-3">Description</dt>
                <dd class="col-sm-9">@(string.IsNullOrWhiteSpace(Model.Description) ? "-" : Model.Description)</dd>
            </dl>
        </div>
    </div>
</div>
>>>>>>> main
